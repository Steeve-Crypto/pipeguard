# pipeguard
Rust CI/CD pipeline security scanner + multi-format config converter (YAML ↔ JSON ↔ TOML). Detects hardcoded secrets, unpinned actions, over-privileged jobs, and common misconfigurations in GitHub Actions / GitLab CI.
